import{consumerOpts,createInbox,JSONCodec,type JetStreamSubscription}from"nats";import{z}from"zod";import{env}from"../config/env.js";import{logger}from"../config/logger.js";import{natsManager}from"../config/nats.js";import{prisma}from"../config/prisma.js";
const envelope=z.object({eventId:z.string().uuid(),eventType:z.literal("monitoring.period.closed.v1"),occurredAt:z.string().datetime(),producer:z.literal("exa-monitoring-service"),aggregateId:z.string().regex(/^\d+$/),version:z.number().int().positive(),data:z.object({monitoringPeriodId:z.string().regex(/^\d+$/),kpiPoolId:z.string().regex(/^\d+$/),poolInputPeriodId:z.string().regex(/^\d+$/),periodKey:z.string(),closedAt:z.string().datetime(),closedWithExceptions:z.boolean()}).passthrough()});const json=JSONCodec<unknown>();
export async function processMonitoringClosedEvent(raw:unknown,subject:string){const event=envelope.parse(raw);if(await prisma.processedEvent.findUnique({where:{eventId:event.eventId}}))return"duplicate" as const;return prisma.$transaction(async tx=>{if(await tx.processedEvent.findUnique({where:{eventId:event.eventId}}))return"duplicate" as const;await tx.monitoringPeriodClosureReference.upsert({where:{kpiPoolId_poolInputPeriodExternalId:{kpiPoolId:BigInt(event.data.kpiPoolId),poolInputPeriodExternalId:BigInt(event.data.poolInputPeriodId)}},create:{monitoringPeriodExternalId:BigInt(event.data.monitoringPeriodId),kpiPoolId:BigInt(event.data.kpiPoolId),poolInputPeriodExternalId:BigInt(event.data.poolInputPeriodId),periodKey:event.data.periodKey,closureType:event.data.closedWithExceptions?"WITH_EXCEPTIONS":"NORMAL",closedAt:new Date(event.data.closedAt),sourceVersion:event.version,sourceEventId:event.eventId},update:{closureType:event.data.closedWithExceptions?"WITH_EXCEPTIONS":"NORMAL",closedAt:new Date(event.data.closedAt),sourceVersion:event.version,sourceEventId:event.eventId}});await tx.processedEvent.create({data:{eventId:event.eventId,eventType:event.eventType,subject,aggregateId:event.aggregateId,sourceService:event.producer}});return"processed" as const;});}
class MonitoringEventsConsumer{private subscription?:JetStreamSubscription;async start(){const js=natsManager.jetStream;if(!js||this.subscription)return;const options=consumerOpts();options.durable(env.NATS_MONITORING_CONSUMER);options.deliverTo(createInbox());options.manualAck();options.ackExplicit();options.deliverAll();options.maxDeliver(10);options.bindStream(env.NATS_MONITORING_STREAM);this.subscription=await js.subscribe("monitoring.period.closed.v1",options);void this.consume(this.subscription);}private async consume(subscription:JetStreamSubscription){for await(const message of subscription)try{await processMonitoringClosedEvent(json.decode(message.data),message.subject);message.ack();}catch(error){if(error instanceof z.ZodError){logger.warn({error},"Invalid Monitoring closure event terminated");message.term();}else{logger.error({error},"Monitoring closure event failed; redelivery requested");message.nak();}}}async stop(){const subscription=this.subscription;this.subscription=undefined;if(subscription)await subscription.destroy();}}
export const monitoringEventsConsumer=new MonitoringEventsConsumer();
