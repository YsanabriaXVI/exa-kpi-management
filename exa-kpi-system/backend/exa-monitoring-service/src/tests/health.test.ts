import request from "supertest"; import { describe,expect,it } from "vitest"; import { createApp } from "../app.js";
describe("Monitoring health",()=>{it("reports liveness",async()=>{const response=await request(createApp()).get("/api/health/live");expect(response.status).toBe(200);expect(response.body).toEqual({status:"live",service:"exa-monitoring-service"});});it("reports readiness without a real database through the test seam",async()=>{const response=await request(createApp({databaseCheck:async()=>{}})).get("/api/health/ready");expect(response.status).toBe(200);expect(response.body.status).toBe("ready");});it("returns 503 when database check fails",async()=>{const response=await request(createApp({databaseCheck:async()=>{throw new Error("down");}})).get("/api/health/ready");expect(response.status).toBe(503);expect(response.body.error.code).toBe("DATABASE_UNAVAILABLE");});});
