import { Router } from "express"; import multer from "multer"; import { approveMonitoringPeriod, closeMonitoringPeriod, confirmExcelImport, downloadExcelTemplate, getResultEntry, listMonitoringPeriods, materializeMonitoringPeriod, previewExcelImport, returnMonitoringPeriod, saveResultEntry, submitMonitoringPeriod, validateMonitoringPeriod } from "../controllers/monitoring-period.controller.js"; const upload=multer({storage:multer.memoryStorage(),limits:{fileSize:10*1024*1024}}); export const monitoringPeriodRouter=Router(); monitoringPeriodRouter.get("/",listMonitoringPeriods); monitoringPeriodRouter.post("/materialize",materializeMonitoringPeriod); monitoringPeriodRouter.get("/:id/result-entry", getResultEntry); monitoringPeriodRouter.post("/:id/result-entry/save-changes", saveResultEntry); monitoringPeriodRouter.get("/:id/result-entry/excel-template",downloadExcelTemplate); monitoringPeriodRouter.post("/:id/result-entry/excel-preview",upload.single("file"),previewExcelImport); monitoringPeriodRouter.post("/:id/result-entry/excel-confirm",confirmExcelImport); monitoringPeriodRouter.post("/:id/validate",validateMonitoringPeriod); monitoringPeriodRouter.post("/:id/submit",submitMonitoringPeriod); monitoringPeriodRouter.post("/:id/return-for-correction",returnMonitoringPeriod); monitoringPeriodRouter.post("/:id/approve",approveMonitoringPeriod); monitoringPeriodRouter.post("/:id/close",closeMonitoringPeriod);
