import request from "supertest";
import { beforeEach,describe,expect,it,vi } from "vitest";
const mocks=vi.hoisted(()=>({batchLookup:vi.fn(),internalCatalog:vi.fn(),list:vi.fn(),get:vi.fn(),create:vi.fn(),update:vi.fn(),deactivate:vi.fn(),softDelete:vi.fn()}));
vi.mock("../services/kpi-configuration.service.js",()=>({kpiConfigurationService:mocks}));
import {app} from "../app.js";
const record={id:1,code:"KPC-049-01",definitionId:49,definitionCode:"KPI-049",definitionName:"Costs",goal:20,measurementUnit:"%",evaluationType:"Lower is better",dataSource:"EMS",ranges:{redFrom:0,redTo:30,yellowFrom:31,yellowTo:65,greenFrom:66,greenTo:100},usedIn:0,status:"CONFIGURED",createdAt:"2026-01-01",createdBy:"System",updatedAt:"2026-01-01",updatedBy:"System",poolNames:[]};
const body={definitionId:49,goal:20,measurementUnit:"%",dataSource:"EMS",ranges:record.ranges,isActive:true};
const approved={...body,targetKind:"ABSOLUTE_TARGET",resultSemantics:"ABSOLUTE_VALUE",evaluationTypeCode:"GREATER_IS_BETTER",scoringMethod:"PROPORTIONAL",scoringRuleConfig:{floorPercent:0,capPercent:100},scoringRuleConfigVersion:1,negativeResultPolicy:"DISALLOW",scoringApprovalStatus:"APPROVED"};
beforeEach(()=>{vi.clearAllMocks();mocks.list.mockResolvedValue({data:[],meta:{page:1,pageSize:20,totalItems:0,totalPages:0}});mocks.internalCatalog.mockResolvedValue({data:[],meta:{page:1,pageSize:20,totalItems:0,totalPages:0}})});
describe("KPI Configuration API",()=>{
 it("lists configurations",async()=>expect((await request(app).get("/api/v1/kpi-configurations")).status).toBe(200));
 it("batch looks up configurations and deduplicates repeated IDs",async()=>{mocks.batchLookup.mockResolvedValue({data:[],notFoundIds:["10","22"]});const response=await request(app).post("/api/v1/kpi-configurations/batch-lookup").send({ids:["10","10","22"]});expect(response.status).toBe(200);expect(mocks.batchLookup).toHaveBeenCalledWith({ids:["10","22"]});expect(response.body.notFoundIds).toEqual(["10","22"])});
 it("exposes a paginated internal catalog",async()=>{const response=await request(app).get("/api/v1/internal/kpi-configurations?page=2&pageSize=10&search=KPI");expect(response.status).toBe(200);expect(mocks.internalCatalog).toHaveBeenCalledWith({page:2,pageSize:10,search:"KPI"})});
 it("rejects an empty batch",async()=>expect((await request(app).post("/api/v1/kpi-configurations/batch-lookup").send({ids:[]})).status).toBe(400));
 it("rejects a batch larger than 100 IDs",async()=>expect((await request(app).post("/api/v1/kpi-configurations/batch-lookup").send({ids:Array.from({length:101},(_,index)=>String(index+1))})).status).toBe(400));
 it("gets a configuration",async()=>{mocks.get.mockResolvedValue(record);expect((await request(app).get("/api/v1/kpi-configurations/1")).body.data.code).toBe(record.code)});
 it("creates a validated configuration",async()=>{mocks.create.mockResolvedValue(record);expect((await request(app).post("/api/v1/kpi-configurations").send(body)).status).toBe(201)});
 it("rejects invalid ranges payload",async()=>expect((await request(app).post("/api/v1/kpi-configurations").send({...body,ranges:{}})).status).toBe(400));
 it("accepts continuous Red, Yellow, Green score ranges",async()=>{mocks.create.mockResolvedValue(record);expect((await request(app).post("/api/v1/kpi-configurations").send({...body,ranges:{redFrom:0,redTo:64,yellowFrom:65,yellowTo:79,greenFrom:80,greenTo:100}})).status).toBe(201)});
 it("publishes only complete approved Greater and Lower proportional configurations",async()=>{mocks.create.mockResolvedValue(record);expect((await request(app).post("/api/v1/kpi-configurations").send(approved)).status).toBe(201);expect((await request(app).post("/api/v1/kpi-configurations").send({...approved,resultSemantics:"UNIT_COST",evaluationTypeCode:"LOWER_IS_BETTER"})).status).toBe(201)});
 it("accepts approved KPI-specific zero-target bands",async()=>{mocks.create.mockResolvedValue(record);const response=await request(app).post("/api/v1/kpi-configurations").send({...approved,goal:0,resultSemantics:"COUNT",evaluationTypeCode:"LOWER_IS_BETTER",scoringMethod:"ZERO_TARGET_BANDS",scoringRuleConfig:{bands:[{minResult:0,maxResult:0,compliance:100},{minResult:1,maxResult:1,compliance:70},{minResult:2,compliance:0}]}});expect(response.status).toBe(201)});
 it("does not approve incomplete zero-target, Equal, Range, Binary or Milestone rules",async()=>{expect((await request(app).post("/api/v1/kpi-configurations").send({...approved,goal:0,scoringMethod:"ZERO_TARGET_BANDS",scoringRuleConfig:{}})).status).toBe(400);expect((await request(app).post("/api/v1/kpi-configurations").send({...approved,scoringMethod:"TOLERANCE",evaluationTypeCode:"EQUAL_IS_BETTER",scoringRuleConfig:{}})).status).toBe(400);expect((await request(app).post("/api/v1/kpi-configurations").send({...approved,scoringMethod:"RANGE_BASED",evaluationTypeCode:"RANGE",scoringRuleConfig:{rangeMin:1}})).status).toBe(400);expect((await request(app).post("/api/v1/kpi-configurations").send({...approved,scoringMethod:"BINARY",resultSemantics:"BINARY",resultMethod:"CALCULATED_FROM_INPUTS",scoringRuleConfig:{states:[]}})).status).toBe(400)});
 it("keeps incomplete business scoring publishable only as BLOCKED",async()=>{mocks.create.mockResolvedValue(record);expect((await request(app).post("/api/v1/kpi-configurations").send({...body,scoringApprovalStatus:"BLOCKED"})).status).toBe(201)});
 it("rejects inverted color ranges",async()=>expect((await request(app).post("/api/v1/kpi-configurations").send({...body,ranges:{redFrom:66,redTo:100,yellowFrom:31,yellowTo:65,greenFrom:0,greenTo:30}})).status).toBe(400));
 it("deactivates a configuration",async()=>{mocks.deactivate.mockResolvedValue({...record,status:"INACTIVE"});expect((await request(app).patch("/api/v1/kpi-configurations/1/deactivate")).body.data.status).toBe("INACTIVE")});
 it("soft deletes without physically deleting the record",async()=>{mocks.softDelete.mockResolvedValue(record);expect((await request(app).delete("/api/v1/kpi-configurations/1")).status).toBe(200);expect(mocks.softDelete).toHaveBeenCalledWith(1n,null)});
});
