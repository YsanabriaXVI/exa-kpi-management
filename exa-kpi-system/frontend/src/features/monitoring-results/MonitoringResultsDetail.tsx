import{useState}from"react";import{useQuery}from"@tanstack/react-query";import{ArrowLeft,Search}from"lucide-react";import{Link,useNavigate,useSearchParams}from"react-router-dom";import{PaginationControls}from"../../components/PaginationControls";import{RowsPerPageSelect}from"../../components/RowsPerPageSelect";import{monitoringReadService}from"./monitoring-results.service";import"./monitoring-results.css";
export function MonitoringResultsDetail(){const navigate=useNavigate(),[params]=useSearchParams();const id=params.get("monitoringPeriodId")??"";const[page,setPage]=useState(1),[pageSize,setPageSize]=useState(10),[search,setSearch]=useState(""),[entryStatus,setEntryStatus]=useState(""),[sortBy,setSortBy]=useState("kpiCode"),[sortOrder,setSortOrder]=useState<"asc"|"desc">("asc");const query=useQuery({queryKey:["monitoring-detail",id,page,pageSize,search,entryStatus,sortBy,sortOrder],queryFn:()=>monitoringReadService.detail(id,{page,pageSize,search,entryStatus:entryStatus?[entryStatus]:[],sortBy,sortOrder}),enabled:Boolean(id),retry:false});const period=query.data?.period;return <main className="monitor-page monitor-detail-page"><nav className="kpi-breadcrumb"><Link to="/app/monitoring-results/overview">Monitoring Overview</Link><span>/</span><span>Monitoring Detail</span></nav><header className="monitor-header"><div><span className="monitor-eyebrow">HISTORICAL PERIOD PROJECTION</span><h1>Monitoring Detail</h1><p>Server-side KPI result detail for one immutable Monitoring Period identity.</p></div></header>{!id?<section className="result-entry-live-state"><p>Select a Monitoring Period from Overview.</p></section>:query.isLoading?<section className="result-entry-live-state"><p>Loading Monitoring Detail…</p></section>:query.isError?<section className="result-entry-live-state"><p>Monitoring Detail could not be loaded.</p><button onClick={()=>query.refetch()}>Retry</button></section>:<><section className="result-entry-live-header"><div><span>{period.code}</span><h1>{period.poolCode} · {period.poolName}</h1><p>{period.periodLabel} · {period.status}</p></div><dl><div><dt>Expected</dt><dd>{period.expected}</dd></div><div><dt>Entered</dt><dd>{period.entered}</dd></div><div><dt>Pending</dt><dd>{period.pending}</dd></div></dl></section><section className="result-entry-live-card"><header><div><h2>KPI Results</h2><p>Filtering, sorting and pagination are executed by Monitoring.</p></div></header><div className="result-toolbar attached-toolbar"><label><Search size={16}/><input value={search} onChange={e=>{setSearch(e.target.value);setPage(1)}} placeholder="Search KPI or ScoreCard…"/></label><select value={entryStatus} onChange={e=>{setEntryStatus(e.target.value);setPage(1)}}><option value="">All entry statuses</option><option value="ENTERED">Entered</option><option value="PENDING">Pending</option></select><select value={sortBy} onChange={e=>setSortBy(e.target.value)}><option value="kpiCode">KPI Code</option><option value="kpiName">KPI Name</option><option value="result">Result</option><option value="score">Score</option><option value="trafficLight">Traffic Light</option></select><button className="entry-secondary" onClick={()=>setSortOrder(v=>v==="asc"?"desc":"asc")}>{sortOrder.toUpperCase()}</button></div><div className="result-entry-live-table-wrap"><table><thead><tr><th>KPI</th><th>ScoreCard</th><th>Unit</th><th>Goal</th><th>Result</th><th>Entry</th><th>Validation</th><th>Score</th><th>Traffic Light</th></tr></thead><tbody>{query.data?.data.map((row:any)=><tr key={row.id}><td><span>{row.kpiCode}</span><strong>{row.kpiName}</strong></td><td>{row.scorecardCode}</td><td>{row.unit??"—"}</td><td>{row.goal??"—"}</td><td>{row.result??"—"}</td><td>{row.entryStatus}</td><td>{row.validation}</td><td>{row.score??"—"}</td><td>{row.trafficLight??"—"}</td></tr>)}</tbody></table><footer className="detail-table-pagination"><span>{query.data?.meta.totalItems??0} KPI lines</span><RowsPerPageSelect value={pageSize} onChange={v=>{setPageSize(v);setPage(1)}}/><PaginationControls page={page} totalPages={Math.max(1,query.data?.meta.totalPages??1)} onPage={setPage} label="Monitoring Detail pagination"/></footer></div></section></>}<button className="monitor-back schedule-bottom-back" onClick={()=>navigate("/app/monitoring-results/overview")}><ArrowLeft size={16}/>Back to Overview</button></main>}
